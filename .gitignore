# Dependencies
node_modules/

# Build output
dist/
.vite/

# Local media + config store (created by the local server at runtime)
uploads/
config/

# Vercel
.vercel/

# Local env
.env
.env.local
*.local

# Editor / OS
.DS_Store
.claude/

# Heavy local recording media (inputs/outputs live at repo root, not committed)
/raw/
/srt/
*.mp4
*.mp3
*.webm
*.mkv
*.mov
*.m4a
*.wav

# Root-level images & artifacts (screenshots, legacy built hub) — not the app.
# Scoped to root so sim assets under src/ are NOT ignored.
/*.png
/*.jpg
/*.jpeg
/hub.html

# Remotion render project (separate tool, has its own deps)
/template/

# TS incremental build info
*.tsbuildinfo

# Android / Gradle (native app build artifacts + machine-local config)
.gradle/
/build/
app/build/
local.properties
*.apk
*.aab
.kotlin/
.cxx/
captures/

# IDE
.idea/
*.iml

# Agent working notes (local memory, not project code)
/memory/
